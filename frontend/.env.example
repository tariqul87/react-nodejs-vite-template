# Base URL for the backend API (no trailing slash). Leave unset in dev when using Vite proxy.
# For production or Docker, set to the backend URL (e.g. http://localhost:3001).
# VITE_API_URL=http://localhost:3001
# VITE_APP_TITLE=React + Node.js Template
